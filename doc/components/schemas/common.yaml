ErrorResponse:
  type: object
  properties:
    error:
      type: string
      example: Stardust not found

ValidationErrorResponse:
  type: object
  properties:
    errors:
      type: array
      items:
        type: string
      example:
        - "Value can't be blank"
        - "Memo is too long"

