extends:
  - recommended

lint:
  extends:
    - recommended
  rules:
    no-unresolved-refs: error
    operation-operationId-unique: error
    path-params-defined: error

decorators:
  - '@redocly/cli/lib/decorators/remove-unused-components'

